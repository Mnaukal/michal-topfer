image: node

before_script:
  - yarn install

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/
    - .next/cache/

build:
  script:
    - yarn run build
  artifacts:
    paths:
      - out
  only:
    - master

deploy:
  script:
    - scp -r -i "$SSH_PRIVATE_KEY" out/index.html topfermi@gimli.ms.mff.cuni.cz:WWW